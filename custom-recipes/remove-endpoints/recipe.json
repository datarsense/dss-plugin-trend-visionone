{
  "meta": {
    "label": "Remove endpoints from inventory",
    "description": "Remove endpoints from Trend Vision One endpoint inventory",
    "icon": "icon-link"
  },

  "kind": "PYTHON",

  "selectableFromDataset" : "Input Dataset",
  "inputRoles" : [
    {
      "name": "Input Dataset",
      "arity": "UNARY",
      "required": true,
      "acceptsDataset": true
    }
  ],
  "outputRoles" : [
    {
      "name": "Output Dataset",
      "arity": "UNARY",
      "required": true,
      "acceptsDataset": true
    }
  ],

  "params": [
    {
      "name": "endpointGuid",
      "label": "Agent GUID column",
      "type": "COLUMN",
      "columnRole":"Input Dataset",
      "mandatory": true
    }
  ],

  "resourceKeys": []
}
